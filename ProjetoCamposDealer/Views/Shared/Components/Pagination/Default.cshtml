@model int

<nav aria-label="Page navigation">
    <ul class="pagination justify-content-center">
        <li class="page-item">
            <a class="page-link" href="@Url.Action(nameof(Index), new { page = 1, name = "", description = ""})">Primeira</a>
        </li>
        @{
            for(int i = 1; i <= Model; i++ )
            {
                <li class="page-item"><a class="page-link" href="@Url.Action(nameof(Index), new { page = i, name = "", description = ""})">@i</a></li>
            }
        }
        <li class="page-item">
            <a class="page-link" href="@Url.Action(nameof(Index), new { page = Model })">Última</a>
        </li>
    </ul>
</nav>
